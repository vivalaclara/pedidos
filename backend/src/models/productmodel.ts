export interface Product {
    id: number;
    name: string;
    description: string;
    price: string;
    available_to_buy: boolean;
  }
//model da tabela produto, definindo os tipos de cada coluna